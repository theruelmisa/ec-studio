import React from 'react';
import { HeroContainer } from './Hero.elements';

const Hero = () => {
    return (
        <HeroContainer>
            HERO 
        </HeroContainer>
    )
};

export default Hero;
